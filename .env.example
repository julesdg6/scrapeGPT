# Ollama connection - set to the URL of your Ollama container or instance
OLLAMA_HOST=http://ollama:11434

# Ollama model to use (must be pulled into Ollama beforehand)
OLLAMA_MODEL=qwen:0.5b

# Ollama vision/multimodal model for image analysis (must be pulled into Ollama beforehand)
# Pull with: ollama pull llava
OLLAMA_VISION_MODEL=llava

# Port to expose the Gradio web UI on the host
GRADIO_PORT=7860

# Telegram Bot token (only required when running ScrapeGoat.py Telegram bot)
TELEGRAM_BOT_TOKEN=

# Path to the shared JSON config file (stores allowed Telegram user IDs, etc.)
# Must point to a persistent volume in Docker; defaults to config.json in the working directory.
CONFIG_PATH=/data/config.json

# Optional: URL of the WhisperLive service for GPU-accelerated speech transcription.
# Set to http://whisperlive:9090 when using the bundled whisperlive service in docker-compose
# (start it with: docker compose --profile whisper up -d).
# Leave empty to disable speech transcription (music analysis still works without it).
# Upgrade from local openai-whisper: set this to your WhisperLive instance URL and
# the openai-whisper package is no longer required.
WHISPERLIVE_HOST=

# SearXNG meta-search engine used as the web scraping/search backend.
# Set to http://searxng:8080 when using the bundled searxng service in docker-compose.
# Set to the URL of your own SearXNG instance for a self-hosted setup.
# Leave empty to fall back to direct proxy scraping (legacy behaviour).
SEARXNG_HOST=http://searxng:8080

# Host port for the SearXNG web UI (only used by the bundled searxng service).
SEARXNG_PORT=8080

# Base URL reported by SearXNG to itself (update if you expose it on a public domain).
SEARXNG_BASE_URL=http://localhost:8080/

# Secret key for the bundled SearXNG instance.
# Generate a strong key with: python3 -c "import secrets; print(secrets.token_hex(32))"
# This MUST be changed for any internet-facing deployment.
SEARXNG_SECRET=please-change-this-secret-key-in-.env

# Optional: Automatic1111 (stable-diffusion-webui) API for image generation.
# Your A1111 container must be started with the `--api` flag.
# Set A1111_HOST to enable the Image Generation tab, e.g. http://a1111 or http://192.168.1.50
A1111_HOST=
# Port A1111 is listening on (default webui is 7860)
A1111_PORT=7860
# Optional: default checkpoint name to use (leave blank to use the currently loaded model)
A1111_MODEL=
# Output resolution (default 256x256)
A1111_WIDTH=256
A1111_HEIGHT=256

# PyTorch build flavour (only used when building the image with docker compose build).
# Leave as 'cpu' for a smaller image (~1.5 GB lighter, no NVIDIA GPU required).
# Set to a CUDA tag (e.g. cu121, cu118) to include NVIDIA GPU support.
# Example: PYTORCH_FLAVOR=cu121
PYTORCH_FLAVOR=cpu

# NVIDIA GPU support: expose all GPUs and driver capabilities to the container.
# Set to 'all' (default) if you have an NVIDIA GPU and the NVIDIA Container Toolkit installed.
# Set to empty to disable GPU access when no NVIDIA GPU is present.
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=all
